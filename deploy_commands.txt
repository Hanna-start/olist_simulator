# requirements.txt만 업데이트하는 경우
git add requirements.txt
git commit -m "Fix requirements.txt for Streamlit Cloud deployment"
git push

# 처음 배포하는 경우 (전체 파일)
# git init
# git add ltv_impact_simulator_v4.py requirements.txt README.md .gitignore
# git commit -m "Initial commit: LTV Impact Simulator v4.0"
# git branch -M main
# git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
# git push -u origin main